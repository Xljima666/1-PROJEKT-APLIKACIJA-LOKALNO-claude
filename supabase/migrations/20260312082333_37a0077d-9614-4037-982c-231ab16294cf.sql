-- Enable leaked password protection (HaveIBeenPwned integration)
-- This is done via auth config, not SQL. Adding a marker comment.
-- The actual setting needs to be enabled via Supabase auth config.
SELECT 1;